<%= turbo_stream.replace "incident_entry_form", partial: "status_pages/incidents/incident_entries/form", locals: { status_page: @incident.status_page, incident: @incident, incident_entry: IncidentEntry.new } %>